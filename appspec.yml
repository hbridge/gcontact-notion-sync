version: 0.0
os: linux
files:
  - source: /
    destination: /var/node/gcontact-node-sync
hooks:
  BeforeInstall:
    - location: scripts/before_install.sh
      timeout: 300
  AfterInstall:
    - location: scripts/after_install.sh
      timeout: 300
  ApplicationStart:
    - location: scripts/start_server.sh
    - runas: root
  
